services:
  nginx:
    image: nginx:latest
    container_name: nginx
    restart: unless-stopped
    environment:
      TZ: Asia/Seoul
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/conf.d:/etc/nginx/conf.d
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
    depends_on:
      - certbot
    networks:
      - kitchen-log

  certbot:
    image: certbot/certbot
    container_name: certbot
    restart: unless-stopped
    volumes:
      - ./data/certbot/conf:/etc/letsencrypt
      - ./data/certbot/www:/var/www/certbot
    entrypoint: >
      /bin/sh -c 'trap exit TERM; while :; do certbot renew --deploy-hook "nginx -s reload"; sleep 12h & wait $${!}; done;'
    networks:
      - kitchen-log

  spring_blue:
    image: seoarc/kitchen-log-server:latest
    container_name: spring_blue
    restart: on-failure:3
    environment:
      SPRING_PROFILES_ACTIVE: prod
      TZ: Asia/Seoul
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      BUCKET_NAME: ${BUCKET_NAME}
      CLOUD_REGION: ${CLOUD_REGION}
      CLOUD_ACCESS_KEY: ${CLOUD_ACCESS_KEY}
      CLOUD_SECRET_KEY: ${CLOUD_SECRET_KEY}
      NAVER_STORAGE_URL: ${NAVER_STORAGE_URL}
      CLOVA_MODEL_URL: ${CLOVA_MODEL_URL}
      CLOVA_API_KEY: ${CLOVA_API_KEY}
      CLIENT_URL: ${CLIENT_URL}
      FIREBASE_KEY: ${FIREBASE_KEY}
    networks:
      - kitchen-log
    volumes:
      - ./:/app/local
      - /var/log/kitchen-log:/var/log/kitchen-log

  spring_green:
    image: seoarc/kitchen-log-server:latest
    container_name: spring_green
    restart: on-failure:3
    environment:
      SPRING_PROFILES_ACTIVE: prod
      TZ: Asia/Seoul
      DB_URL: ${DB_URL}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      JWT_SECRET_KEY: ${JWT_SECRET_KEY}
      NAVER_STORAGE_ACCESS_KEY: ${NAVER_STORAGE_ACCESS_KEY}
      NAVER_STORAGE_SECRET_KEY: ${NAVER_STORAGE_SECRET_KEY}
      NAVER_STORAGE_URL: ${NAVER_STORAGE_URL}
      CLOVA_MODEL_URL: ${CLOVA_MODEL_URL}
      CLOVA_API_KEY: ${CLOVA_API_KEY}
      CLIENT_URL: ${CLIENT_URL}
      FIREBASE_KEY: ${FIREBASE_KEY}
    networks:
      - kitchen-log
    volumes:
      - ./:/app/local
      - ./logs:/var/log/spring

  promtail:
    image: grafana/promtail:latest
    container_name: promtail
    volumes:
      - ./promtail-config.prod.yml:/etc/promtail/config.yml
      - /var/log/kitchen-log:/var/log/app
      - /var/lib/promtail:/var/lib/promtail
    command: -config.file=/etc/promtail/config.yml
    networks:
      - kitchen-log

networks:
  kitchen-log:
    driver: bridge
